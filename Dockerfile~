# Use the official OpenJDK image as the base image
FROM openjdk:17-jdk-slim

# Set the working directory in the container
WORKDIR /app

# Copy the Maven wrapper and the project's pom.xml file to the container
COPY mvnw .
COPY .mvn .mvn
COPY pom.xml .

# Download the project dependencies using the Maven wrapper
RUN ./mvnw dependency:go-offline

# Copy the project source code to the container
COPY src ./src

# Build the project using the Maven wrapper
RUN ./mvnw package -DskipTests

# Expose the port on which your application will run (adjust if necessary)
EXPOSE 8080

# Set the command to run your application when the container starts
# Use an environment variable to specify the active profile
CMD ["java", "-jar", "target/domerka-1.0.0-SNAPSHOT.jar"]